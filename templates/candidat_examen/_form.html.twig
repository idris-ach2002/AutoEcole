<div class="card form-card">{{ form_start(form) }}<div class="grid grid-2">{{ form_row(form.candidat) }}{{ form_row(form.examen) }}{{ form_row(form.statut) }}{{ form_row(form.resteAPayer) }}</div><div class="actions"><button class="btn primary">{{ button_label|default('Enregistrer') }}</button><a class="btn" href="{{ path('app_candidat_examen_index') }}">Annuler</a></div>{{ form_end(form) }}</div>
