<div class="card form-card">{{ form_start(form) }}<div class="grid grid-2">{{ form_row(form.typeExamen) }}{{ form_row(form.datePassage) }}{{ form_row(form.lieu) }}{{ form_row(form.frais) }}{{ form_row(form.statutExamen) }}</div><div class="actions"><button class="btn primary">{{ button_label|default('Enregistrer') }}</button><a class="btn" href="{{ path('app_examen_index') }}">Annuler</a></div>{{ form_end(form) }}</div>
