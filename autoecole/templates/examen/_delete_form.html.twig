<form method="post" action="{{ path('app_examen_delete', {'id': examen.id}) }}" style="display:inline;" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examen.id) }}">
    <button class="btn btn-sm btn-danger">🗑️ Supprimer</button>
</form>
