<form method="post" action="{{ path('app_candidat_delete', {'id': candidat.id}) }}" style="display:inline;" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidat.id) }}">
    <button class="btn btn-sm btn-danger">🗑️ Supprimer</button>
</form>
